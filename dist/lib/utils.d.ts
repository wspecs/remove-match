export declare function findMatch(filename: string, match: string): {
    nonMatchedLines: string[];
    matchedLinesIndex: number[];
};
export declare function writeOutput(filename: string, lines: string[]): void;
